class UserController < ApplicationController
  def editer
  	# @user = user.new(first_name: params[:first_name], last_name: params[:last_name], status: params[:status])
  	# if @user.save
  	# 	redirect_to: "user/afficher"
  end

  def afficher
  end
end
